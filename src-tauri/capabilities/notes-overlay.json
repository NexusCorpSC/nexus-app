{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "notes-overlay",
  "description": "Notes overlay. It has no decorations, so its header doubles as the title bar and the webview has to be allowed to drag the window — `core:default` does not cover that.",
  "windows": ["notes"],
  "permissions": ["core:window:allow-start-dragging"]
}
