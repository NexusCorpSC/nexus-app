{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "plan-overlay",
  "description": "Plan de vol overlay. Like the squad one it has no decorations, so its header doubles as the title bar and the webview has to be allowed to drag the window — `core:default` does not cover that. The rest, the API among it, comes from `default`, whose window list it joins.",
  "windows": ["plan"],
  "permissions": ["core:window:allow-start-dragging"]
}
