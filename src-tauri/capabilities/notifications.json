{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "notifications",
  "description": "Notification overlays, corner and top. They only render what Rust hands them and report back the size of their stack, so they need neither network nor storage access.",
  "windows": ["notifications", "banners"],
  "permissions": ["core:default"]
}
