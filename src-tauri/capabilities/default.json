{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window, quick-search overlay and notes overlay: they call the Nexus Tools API and read the persisted settings.",
  "windows": ["main", "overlay", "notes"],
  "permissions": [
    "core:default",
    "opener:default",
    "store:default",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "https://tools.services.nexus/*" },
        { "url": "http://localhost:3000/*" },
        { "url": "http://127.0.0.1:3000/*" }
      ]
    }
  ]
}
