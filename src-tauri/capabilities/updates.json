{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updates",
  "description": "Self-update, from the main window only: it is the one window with an interface to propose an update in, and the only one that ever installs one.",
  "windows": ["main"],
  "permissions": ["updater:default"]
}
